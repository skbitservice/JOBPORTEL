{
  "name": "premium-job-application-platform",
  "version": "1.0.0",
  "private": true,
  "description": "A modern full-stack recruitment website with applicant registration, uploads, admin dashboard, and email notifications.",
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
